# ─── Sustainability Signals – Environment Variables ──────────────────────────
# Copy this file to .env and fill in the values.
# NEVER commit the .env file itself.

# ── Cloudflare (required for full-stack dev with Pages Functions) ──────────
CLOUDFLARE_API_TOKEN=
CLOUDFLARE_ACCOUNT_ID=

# ── Admin panel ───────────────────────────────────────────────────────────────
# SHA-256 hash of the admin password (client-side gate).
# Generate:  echo -n 'yourpassword' | sha256sum   (Linux/macOS)
#   or:      [System.BitConverter]::ToString((New-Object System.Security.Cryptography.SHA256Managed).ComputeHash([System.Text.Encoding]::UTF8.GetBytes("yourpassword"))).Replace("-","").ToLower()   (PowerShell)
VITE_ADMIN_PASSWORD_HASH=

# ── FinBERT-ESG inference (optional) ──────────────────────────────────────────
# Base URL of your hosted FinBERT inference server (e.g. Azure Container Apps)
FINBERT_URL=
# Optional API key; if set, Pages Functions send x-api-key header
FINBERT_API_KEY=
